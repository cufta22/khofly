import PagePrivateView from "src/modules/PrivateView";
import { ROOT_META_FUNCTION } from "app/meta/root";

// Meta tags
export const meta = ROOT_META_FUNCTION;

const PrivateView = () => {
  return <PagePrivateView />;
};

export default PrivateView;
